{
  "name": "@xennis/websites",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "format:check": "prettier --check --ignore-path .gitignore .",
    "format": "prettier --write --ignore-path .gitignore ."
  },
  "devDependencies": {
    "prettier": "^3",
    "prettier-plugin-tailwindcss": "^0.5.14",
    "typescript": "^5"
  }
}
